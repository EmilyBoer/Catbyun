var showingTime = [["Die hard", 445] ["Castaway", 700] ["Minions", 1200]]

var notShowing = ["American Hustle", "Speed", "Amy", "End of the Tour"]

movieChoice = "Speed"

var ticket = buildTicket(showingTime, notShowing, movieChoice )

playTime.map(function(movie) {
  if (movie == playTime][0]); {
return ("your movie is playing at" + playtime[1])
  }
  else if  (movie == notShowing[0]); {
return ("your movie is not playing tonight")
  }

};